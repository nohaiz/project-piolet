<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/home.css">
    <title>Home Page</title>
    <%-include('partials/nav.ejs')%>
</head>
<body>
    <main>
        <!-- LEFT SIDE -->
        <section>
            <div class="section-left">
                <% if (user) { %>
                    <% if (isShowing) { %>
                        <%-include('partials/projects/index.ejs', {userProjs: userProjs})%>
                    <% } %>
                <% } %>
                <p>This is something u could use for the empty space</p>
            </div>
        </section>

        <!-- MIDDLE -->
        <section>
            <div class="section-mid">
                <h1>Project Piolet</h1>
          <p>"Project Piolet is a user-friendly web tool for organizing tasks and projects efficiently. It features a visual board system with movable task cards, collaboration tools, and customization options, making it perfect for managing personal tasks or team projects."</p>
            <% if (user) { %>
                <p><a href="/users/<%=user._id%>/projects/new">Project +</a></p>
                <% if (isNewLinkClicked) { %>
                    <%-include('partials/projects/new.ejs')%>
                <% } %>
            <% } %>
            </div>
        </section>
        
        <!-- RIGHT SIDE -->
        <section>
            <div class="section-right">
                <p>hi</p>
            </div>
        </section>

    </main>
</body>
</html>