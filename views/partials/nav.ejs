<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>
    <nav class="nav-menu">
        <div class="nav-left">
            <a href="/">Project Piolet</a>
            <% if (user) { %>
                <a href="">Dashboard</a>
            <% } %>
        </div>
        <div class="nav-right">
            <% if (user) { %>
                <a href="/auth/sign-out">Sign Out</a>
            <% } else { %>
                <a href="/auth/sign-up">Sign Up</a>
                <a href="/auth/sign-in">Sign In</a>
            <% } %>
        </div>
    </nav>
</body>
</html>