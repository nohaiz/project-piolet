<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>
    <div class="section-left-appear">
        <% userProjs.forEach((proj) => { %>
            <a class="section-left-text" href="/users/userId>/projects/<%=proj._id%>"><%= proj.title %></a>
        <% }) %> 
    </div>
</body>
</html>